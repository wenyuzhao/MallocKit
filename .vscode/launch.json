{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Hoard",
            "type": "lldb",
            "request": "launch",
            "preLaunchTask": "Build",
            "cwd": "${workspaceRoot}",
            "env": {
                "LD_PRELOAD": "${workspaceRoot}/target/debug/libhoard.so"
            },
            "program": "ls",
            "args": [
                "-a"
            ],
        }
    ]
}