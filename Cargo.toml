[workspace]
members = [
    "mallockit",
    "mallockit/proc_macro",
    "bump",
    "buddy",
    "sanity",
]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
codegen-units = 1